stages:
  - build
  - test
  - deploy

default:
  image:
    name: debian:11
    pull_policy: if-not-present
  tags:
    - docker
  become: true

build_oscap:
  stage: build
  script:
    - pwd
    - ls -la
    - build_dependencies/test.sh
  artifacts:
    paths:
      - oscap_${openscap_version}.deb
test_oscap:
  stage: test
  needs: ["build_oscap"]
  script:
    - echo testing
    # - apt update
    # - apt install ./oscap_${openscap_version}.deb
    # - oscap --version
